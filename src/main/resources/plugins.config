# A single .jar file can contain multiple plugins, specified in separate lines.
#
# The format is: <menu>, "<menu label>", <class name>
#
# If something like ("<arg>") is appended to the class name, the setup() method
# will get that as arg parameter; otherwise arg is simply the empty string.

Plugins>INRA, "ATP Edge Detection", fr.isima.inra.plugins.ATP_Raising_Edge
Plugins>INRA, "Immuno Edge Detection", fr.isima.inra.plugins.Immuno_Raising_Edge
Plugins>INRA, "Cells Segmentation", fr.isima.inra.plugins.Cells_Segmentation
Plugins>INRA, "Cells classification", fr.isima.inra.plugins.Cells_Classification
